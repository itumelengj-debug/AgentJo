# Agent Jo — what it needs to run.
# Pinned loosely: these are stable APIs, and pinning hard means a security
# fix in a dependency needs a release here.

# engines
anthropic>=0.40
openai>=1.50
httpx>=0.27,<0.28

# the web app
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
rich>=13.7

# reading what you attach
pypdf>=4.2
pdfplumber>=0.11
python-docx>=1.1
openpyxl>=3.1
python-pptx>=0.6.23
beautifulsoup4>=4.12
odfpy>=1.4

# release signing and verification
cryptography>=42.0

# optional, and it says so when they're missing:
#   playwright        - portal applications and reading JavaScript pages
#                       (installed by setup, along with its browser)
#   faster-whisper    - voice input
#   pytesseract       - text out of images
