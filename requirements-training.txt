# Fine-tuning stack (separate from the lightweight runtime requirements).
# Tested ranges — pin tighter once your setup works.
torch>=2.3
transformers>=4.46,<4.58
datasets>=3.0
peft>=0.13,<0.17
trl>=0.12,<0.16
accelerate>=1.0
# QLoRA 4-bit (CUDA GPUs; skip on Mac/CPU — script falls back automatically):
bitsandbytes>=0.44; platform_system == "Linux"
